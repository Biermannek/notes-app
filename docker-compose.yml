services:
  notes-app:
    image: ghcr.io/biermannek/notes-app:latest
    container_name: notes-app
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - notes_data:/data
    environment:
      - DB_PATH=/data/notes.db
      - SECRET_KEY=zmen-me-na-nejaky-tajny-retezec-minimalne-32-znaku

volumes:
  notes_data:
